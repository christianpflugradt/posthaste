name: QA Gates

on:
  pull_request:
    paths:
      - "src/**"
      - "scripts/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "Makefile"
      - ".github/workflows/**"
  push:
    branches:
      - main
    paths:
      - "src/**"
      - "scripts/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "Makefile"
      - ".github/workflows/**"

jobs:
  qa-matrix:
    strategy:
      fail-fast: false
      matrix:
        include:
          - target_name: linux-x86_64
            runs_on: ubuntu-latest
          - target_name: windows-x86_64
            runs_on: windows-latest
          - target_name: macos-arm64
            runs_on: macos-14
    runs-on: ${{ matrix.runs_on }}
    steps:
      - name: Checkout
        uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7

      - name: Cargo fmt check
        run: cargo fmt --check

      - name: Cargo clippy (deny warnings)
        run: cargo clippy -- -D warnings

      - name: Cargo test
        run: cargo test

      - name: Trigger regression checks
        run: ./scripts/check_ci_trigger_filters.sh

      - name: Collect diagnostics (non-Windows)
        if: runner.os != 'Windows'
        run: |
          {
            echo "target=${{ matrix.target_name }}"
            echo "runner_os=${{ runner.os }}"
            echo "runner_arch=${{ runner.arch }}"
            rustc -vV
            cargo -V
          } > diagnostics.txt

      - name: Collect diagnostics (Windows)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          "target=${{ matrix.target_name }}" | Out-File -FilePath diagnostics.txt
          "runner_os=${{ runner.os }}" | Out-File -FilePath diagnostics.txt -Append
          "runner_arch=${{ runner.arch }}" | Out-File -FilePath diagnostics.txt -Append
          rustc -vV | Out-File -FilePath diagnostics.txt -Append
          cargo -V | Out-File -FilePath diagnostics.txt -Append

      - name: Upload diagnostics
        if: always()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02
        with:
          name: diagnostics-${{ matrix.target_name }}
          path: diagnostics.txt
          retention-days: 7
