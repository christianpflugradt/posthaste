test: add regression harness for tasks helper output

Add deterministic tests for tasks listing behavior and wire them into
CI governance checks.

- add scripts/test_tasks.sh with role, limit, and no-match cases
- add TASKS_ROOT support in tasks helper for isolated fixtures
- add make tasks-test target
- run tasks-test in governance-checks workflow

Role: Product Owner
