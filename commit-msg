feat: add repo-managed pre-commit hook installation

Add a tracked pre-commit hook that runs `make preflight` and provide an
installer command so local manual commits are blocked on backlog/index
sync drift.

- add .githooks/pre-commit
- add scripts/install_hooks.sh
- add make hooks-install target
- document hook installation in README

Role: Implementing Developer
