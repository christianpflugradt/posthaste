feat: add deterministic tasks helper for backlog pickup visibility

Add a lightweight tasks helper so agents can produce a concise,
repeatable top-5 backlog view for the active role without ad-hoc parsing.

- add scripts/tasks.sh (role-filtered deterministic listing)
- add make tasks target with ROLE/LIMIT parameters
- wire helper into interaction docs and runtime contract
- append policy changelog entry for this productivity improvement

Role: Product Owner
