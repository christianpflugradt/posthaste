fix: harden governance guard changed-file parsing

Improve governance guard reliability by replacing brittle whitespace-based
file extraction with null-delimited porcelain parsing.

- parse changed files via git status --porcelain=v1 -z
- handle rename/copy records safely
- keep canonical/runtime/changelog guard checks unchanged
- append policy changelog entry for parser hardening

Role: Product Owner
