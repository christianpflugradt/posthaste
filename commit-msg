docs: require role trailer in commit messages

Persist commit metadata policy so role traceability is captured in
commit bodies without breaking conventional-commit tooling.

- add required `Role: <Role Name>` trailer rule
- add recommended `Backlog: <PB-xxxx>` trailer rule
- update AGENTS and bootstrap prompt to enforce the same behavior

Role: Product Owner
