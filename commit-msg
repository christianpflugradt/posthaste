chore: add preflight consistency checks for backlog pickup

Add deterministic preflight validation to catch stale or inconsistent
work-selection state before each go work unit.

- add scripts/preflight.sh for index/item consistency checks
- add Makefile preflight target with optional ROLE filter
- require preflight in AGENTS, backlog pickup rules, prompt, and README guidance
- sync existing index drifts so preflight passes on current backlog

Role: Product Owner
