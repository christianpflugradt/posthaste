feat: add index-sync tool for deterministic backlog row updates

Add a helper to compute and apply index row fields from per-issue gate
state, reducing manual drift in backlog/index.md.

- add scripts/index_sync.sh
- derive status, next eligible roles, blocker, and updated date
- support optional PB filter via environment variable
- add make index-sync target

Role: Product Owner
