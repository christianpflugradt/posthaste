fix: enforce preflight in commit script before commit/push

Prevent out-of-sync backlog/index state from being committed via the
standard automation path by running `make preflight` at the start of
`./commit.sh`.

Also synced PB-0012 index row so the repository returns to a passing
preflight baseline.

Role: Implementing Developer
